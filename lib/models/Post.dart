class Post{
  final String title;
  final String description;
  final String startDate;
  final String endDate;
  final String uid;

  Post(this.title, this.description, this.startDate, this.endDate, this.uid);
  
}